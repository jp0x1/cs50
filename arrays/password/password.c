#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    char password[50];
    int counter = 0;
    printf("Please enter a password: ");
    scanf("%s", password);
    //password checks
    //check if one uppercase letter
    for (int i=0; i < strlen(password); i++){
        
    }
}

